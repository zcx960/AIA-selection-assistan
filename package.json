{
  "name": "selection-assistant-lite",
  "version": "0.7.0",
  "description": "轻量跨平台 AI 划词助手，支持 OpenAI-compatible 与 Anthropic API。",
  "main": "out/main/index.js",
  "author": "selection-assistant-lite contributors",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zcx960/AIA-selection-assistan.git"
  },
  "bugs": {
    "url": "https://github.com/zcx960/AIA-selection-assistan/issues"
  },
  "homepage": "https://github.com/zcx960/AIA-selection-assistan#readme",
  "keywords": [
    "electron",
    "selection",
    "ai",
    "desktop-app",
    "openai-compatible",
    "anthropic",
    "translation",
    "productivity"
  ],
  "type": "module",
  "scripts": {
    "dev": "electron-vite dev",
    "start": "electron-vite preview",
    "build": "pnpm typecheck && electron-vite build",
    "prepare:selection-hook": "node scripts/prepare-selection-hook.js",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json",
    "test": "vitest run",
    "dist:mac": "pnpm build && pnpm prepare:selection-hook && electron-builder --mac dmg zip --publish never",
    "dist:win": "pnpm build && pnpm prepare:selection-hook && electron-builder --win nsis portable --x64 --publish never",
    "dist:win:x64": "pnpm build && pnpm prepare:selection-hook && electron-builder --win nsis portable --x64 --publish never",
    "dist:win:arm64": "pnpm build && pnpm prepare:selection-hook && electron-builder --win nsis portable --arm64 --publish never"
  },
  "dependencies": {
    "@electron-toolkit/preload": "^3.0.0",
    "@electron-toolkit/utils": "^4.0.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "electron-store": "^8.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "selection-hook": "2.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "electron-vite": "^2.3.0",
    "typescript": "^5.7.2",
    "vite": "^5.4.21",
    "vitest": "^2.1.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild",
      "selection-hook"
    ]
  }
}
